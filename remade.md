# monorepo-starter

monorepo starter template

## install

```shell

pnpm install

```

## release

Release your package. use `pnpm release` and step into the command line.

```shell

pnpm release

```
